var name = Object.keys(fieldInfos)[0],
	fieldInfo = fieldInfos[name],
	clipPath = clipPaths[name],
	contaminatorSize = contaminatorSets[name].size,
	contaminators = contaminatorSets[name].contaminators,
	settings = fieldSettings[name];